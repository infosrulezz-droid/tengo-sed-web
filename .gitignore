# Secrets
.env
.env.local

# Backups (don't need in git)
backups/

# OS
.DS_Store
Thumbs.db

# Node
node_modules/

# Third-party template clone (has its own .git — would break the push)
n8n-free-templates/

# Business data, not code
problemas_inventario_*.xlsx

# Service-account keys generated by social-posting/setup-service-account.sh
*-key.json
n8n-social-key.json
dist/
